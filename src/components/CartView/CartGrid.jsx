import { useEffect } from "react";
import { getCards } from "../../helpers/getCards.js";

export const CartGrid = () => {
  
  useEffect(() => {
    first
    getCards();
  
  }, [])
  

  return <div></div>;
};

export const CardItem = ({ latitude, longitude }) => {

  console.log({ latitude, longitude });

  return (
    <div>
      <div className="card">
        <img src="#" alt="cardimage"/>
      </div>
    </div>
  )
}
